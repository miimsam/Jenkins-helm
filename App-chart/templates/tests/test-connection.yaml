apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "App-chart.fullname" . }}-test-connection"
  labels:
    {{- include "App-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "App-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
